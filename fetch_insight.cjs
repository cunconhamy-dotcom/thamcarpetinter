const https = require('https'); https.get('https://carpetsinter.com/aspekt-insight/', res => { let data = ''; res.on('data', chunk => data += chunk); res.on('end', () => { const regex = /https:\/\/carpetsinter\.com\/wp-content\/uploads\/[a-zA-Z0-9\-\_\.]+\.(jpg|jpeg|png)/gi; const matches = data.match(regex); if (matches) { const unique = [...new Set(matches)]; console.log(unique); } }); });
