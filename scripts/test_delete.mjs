import { createClient } from '@supabase/supabase-js'; import { config } from 'dotenv'; config({ path: '.env.local' }); const supabaseUrl = process.env.VITE_SUPABASE_URL; const supabaseServiceKey = process.env.SUPABASE_SERVICE_ROLE_KEY; const supabase = createClient(supabaseUrl, supabaseServiceKey); async function run() { console.log('Testing delete...'); const { data } = await supabase.from('collections').select('*').limit(1); if(!data || data.length === 0) return console.log('No collections.'); console.log('Trying to delete with Service Role (should bypass RLS):', data[0].id); const { error } = await supabase.from('collections').delete().eq('id', data[0].id); console.log('Error:', error); } run();